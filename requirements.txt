# Nano Banana - Python Dependencies
#
# ZERO REQUIRED DEPENDENCIES - Uses Python stdlib only!
# The scripts use urllib.request instead of requests library,
# eliminating PEP 668 issues on modern systems.

# Optional - for .env file support (not required if using exported keys)
# python-dotenv>=0.19.0

# Note: Set your API key as an environment variable:
#   export OPENROUTER_API_KEY='sk-or-v1-your-key-here'
